/**
 * Shared runtime settings helpers.
 *
 * Section 1/2 intentionally exposes only the simplified foreground/background
 * subagent configuration surface. Legacy workflow-engine settings were removed.
 */

export {};
